# MS Word
*.doc
*.docx
*.dot
*.dotx
*.docm
*.dotm

# Local config (contains tokens, never commit)
config.local.json

# Generated (credentials inlined in each page)
comments.js

# Build artifacts
build_venv/
dist/
build/
__pycache__/

# Compiled executables — prevent any exe from being tracked
*.exe
*.EXE

# PyInstaller artifacts
*.spec

# Named artifacts (match directory and file)
SiteTools
SiteTools.exe

# CKEditor bundle (1.4MB JS, never commit — copied from bundled assets)
ckeditor/
site/
site_tools.config
